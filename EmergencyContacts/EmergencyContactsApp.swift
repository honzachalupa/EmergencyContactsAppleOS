import SwiftUI
import SwiftData

@main
struct EmergencyContactsApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        // .modelContainer(for: [DataItem_.self])
    }
}
